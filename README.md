example script
```
const {addControllerToMyNeurons} = await import("https://unpkg.com/yolosns-2@1.1.1/dist/esm/index.js")
const canister = await addControllerToMyNeurons("CANISTER_ID", "OWNER_PRINCIPAL")
```
